# Spell Checker
Use this to scan a directory and all files inside for commonly misspelled words.

## Run
Install Python and run the following:
```python
python app.py --path /path/to/directory
```

It will print any files with misspelled words and the words found.